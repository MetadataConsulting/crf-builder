subprojects {
    apply plugin: 'groovy'

    repositories {
       jcenter()
    }

    dependencies {
        testCompile 'org.spockframework:spock-core:1.0-groovy-2.4'
    }

    version = '0.1'

    jar {
        manifest.attributes provider: 'gradle'
    }
}
